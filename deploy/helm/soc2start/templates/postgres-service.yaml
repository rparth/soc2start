{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "soc2start.fullname" . }}-postgres
  labels:
    {{- include "soc2start.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
  selector:
    {{- include "soc2start.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
{{- end }}
